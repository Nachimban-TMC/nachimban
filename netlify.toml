# Netlify 배포 설정. 빌드는 없고(정적), site/ 폴더를 그대로 게시.
# site/index.html 이 매일 GitHub Actions 로 갱신되면 Netlify 가 자동 재배포합니다.
[build]
  publish = "site"
  command = ""

# 루트 접속 시 index.html 로
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
